#' @param overwrite if TRUE the file will be saved and overwritten. Otherwise,
#' only the logging is triggered. Note that, depending on
#' `ask_before_applying_changes`, the user is ask whether the result is
#'  as desired.
